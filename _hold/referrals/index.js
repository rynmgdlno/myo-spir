import Head from "next/head";

import styles from "./referrals.module.scss";

const Referrals = () => {
  return (
    <main>
      <Head>
        <title>Referrals</title>
        <meta name="Referrals" content="" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div>

      </div>
    </main>
  );
};

export default Referrals